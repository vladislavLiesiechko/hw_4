"use strict"

function getArithmeticMean(firstNumber, secondNumber, thirdNumber){
    return (firstNumber + secondNumber + thirdNumber) / 3;
}
let result= getArithmeticMean(1, 2, 3);

console.log(result);